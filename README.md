This is the DappControl contract for the Atlas RFQ system. The DappControl can be used by both EOAs and Smart Wallets.

Set up the environment variables: `MAINNET_RPC_URL`

To run the tests:
```
forge test
```